package IntegerMasive;

import java.util.Arrays;

public class Main extends InOrder{
    public static void main (String [] arg) {
       InOrder inOrder = new InOrder();
       System.out.println(Arrays.toString(inOrder.order()));
      }
}
